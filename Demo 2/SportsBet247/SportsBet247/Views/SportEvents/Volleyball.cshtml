@model SportsBet247.ViewModels.SportEventsViewModel

@{
    this.ViewData["Title"] = "Volleyball events";
}

<div class="text-center font-weight-bold btn-secondary">
    <table class="table">
        <thead class="btn-success">
            <tr>
                <th>Home team</th>
                <th>Odd</th>
                <th>Away team</th>
                <th>Odd</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var volleyballEvent in Model.SportEvents)
            {
                <tr class="table-striped col-md-12">
                    <td>
                        @volleyballEvent.HomeTeamName
                    </td>
                    <td class="btn-warning">
                        @volleyballEvent.HomeTeamOddView
                    </td>
                    <td>
                        @volleyballEvent.AwayTeamName
                    </td>
                    <td class="btn-warning">
                        @volleyballEvent.AwayTeamOddView
                    </td>
                    <td>
                        @volleyballEvent.PlayedOn
                    </td>
                </tr>
            }
    </table>
</div>
